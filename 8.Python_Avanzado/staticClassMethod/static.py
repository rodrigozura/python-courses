class Calculator:
    # Este método es un método estático, es decir, no necesita
    # una instancia de la clase para ejecutarse
    @staticmethod
    def add(a: int, b: int) -> int:
        return a + b